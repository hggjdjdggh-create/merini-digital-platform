import { getDictionary } from '../layout'; import { PageShell } from '@/components/seo/JsonLd';
export default function Pricing({ params }: { params: { locale: 'en'|'fr'|'ar'|'es' } }) { const d=getDictionary(params.locale); return <PageShell><h1 className="text-5xl font-bold">{d.pricing.title}</h1><p className="mt-5 text-lg text-black/65">{d.pricing.intro}</p><div className="mt-14 grid gap-5 md:grid-cols-4">{d.pricing.plans.map(([name,price,features])=><article key={name} className="border border-black/10 bg-white p-6"><h2 className="text-xl font-semibold">{name}</h2><p className="mt-5 text-3xl font-bold">{price}</p><ul className="mt-6 space-y-3 text-sm text-black/65">{features.map((f)=><li key={f}>— {f}</li>)}</ul></article>)}</div></PageShell>; }
