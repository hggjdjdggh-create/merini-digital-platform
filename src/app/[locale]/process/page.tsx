import { getDictionary } from '../layout'; import { PageShell } from '@/components/seo/JsonLd';
export default function Process({ params }: { params: { locale: 'en'|'fr'|'ar'|'es' } }) { const d=getDictionary(params.locale); return <PageShell><h1 className="text-5xl font-bold">{d.process.title}</h1><div className="mt-14 grid gap-5 md:grid-cols-5">{d.process.steps.map(([title,text],i)=><article key={title} className="border border-black/10 bg-white p-6"><span className="text-sm text-[#b85c3b]">0{i+1}</span><h2 className="mt-5 font-semibold">{title}</h2><p className="mt-3 text-sm text-black/60">{text}</p></article>)}</div></PageShell>; }
